// Declare immutable and mutable variables
let x = 10;
let mut y = 20;

// Define a function
func add(a: int, b: int): int {
    return a + b;
}

// Use the function
let result = add(x, y);

// Conditional logic
if (result > 25) {
    print("Greater!");
} else {
    print("Smaller or equal.");
}
